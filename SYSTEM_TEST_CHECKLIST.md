# ✅ System Test Checklist

## 🔍 ตรวจสอบระบบให้สมบูรณ์

### 1. Departments (แผนก)
- [x] ✅ สร้างแผนกได้ (หน้า `/admin/departments/new`)
- [ ] ❌ แก้ไขแผนก (ไม่มีหน้า `/admin/departments/[id]`)
- [ ] ❌ ลบแผนก (ไม่มี UI)
- [ ] ❌ แสดงรายการแผนก (มีแล้ว)
- [ ] ❌ Inline creation ในหน้าเพิ่มพนักงาน (มีแล้ว แต่ต้องตรวจสอบว่า sync)

### 2. Positions (ตำแหน่ง)
- [ ] ❌ สร้างตำแหน่งได้ (ไม่มีหน้า `/admin/positions/new`)
- [ ] ❌ แก้ไขตำแหน่ง (ไม่มีหน้า `/admin/positions/[id]`)
- [ ] ❌ ลบตำแหน่ง (ไม่มี UI)
- [x] ✅ แสดงรายการตำแหน่ง (มีแล้ว)
- [ ] ❌ Inline creation ในหน้าเพิ่มพนักงาน (มีแล้ว แต่ต้องตรวจสอบว่า sync)

### 3. Employees (พนักงาน)
- [x] ✅ สร้างพนักงานได้ (หน้า `/admin/employees/new`)
- [ ] ❌ แก้ไขพนักงาน (ไม่มีหน้า `/admin/employees/[id]`)
- [ ] ❌ ลบพนักงาน (ไม่มี UI)
- [x] ✅ แสดงรายการพนักงาน (มีแล้ว)
- [x] ✅ Invite Code generation (มีแล้ว)

### 4. Data Persistence
- [ ] ❌ ตรวจสอบว่าแผนกถูกเก็บใน database จริง
- [ ] ❌ ตรวจสอบว่าตำแหน่งถูกเก็บใน database จริง
- [ ] ❌ ตรวจสอบว่าพนักงานถูกเก็บใน database จริง
- [ ] ❌ ตรวจสอบว่า org_id ถูก set ถูกต้อง

### 5. Permissions & RLS
- [ ] ❌ ตรวจสอบ permissions สำหรับ departments.manage
- [ ] ❌ ตรวจสอบ permissions สำหรับ positions.manage
- [ ] ❌ ตรวจสอบ permissions สำหรับ employees.create
- [ ] ❌ ตรวจสอบ RLS policies

### 6. Error Handling
- [ ] ❌ ตรวจสอบ error messages
- [ ] ❌ ตรวจสอบ validation
- [ ] ❌ ตรวจสอบ loading states

---

## 🐛 ปัญหาที่พบ

1. **ไม่มีหน้า Edit/Delete สำหรับ Departments และ Positions**
2. **ไม่มีหน้า `/admin/positions/new`**
3. **ไม่มีหน้า Detail/Edit สำหรับ Employees**
4. **Link ไปหน้าที่ไม่มีอยู่ (404 errors)**

---

## 🔧 ต้องแก้ไข

1. สร้างหน้า `/admin/departments/[id]` สำหรับ edit/delete
2. สร้างหน้า `/admin/positions/new` สำหรับสร้างตำแหน่ง
3. สร้างหน้า `/admin/positions/[id]` สำหรับ edit/delete
4. สร้างหน้า `/admin/employees/[id]` สำหรับดู/แก้ไข/ลบ
5. เพิ่ม delete buttons ใน list pages
6. ตรวจสอบ data persistence
7. ตรวจสอบ permissions
